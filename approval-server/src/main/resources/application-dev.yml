jwt:
  secret: jwt  # JWT密钥
  expiration: 864000000             # JWT过期时间（毫秒）

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/approval_system_backend
    username: Hunger
    password:

  data:
    redis:
      host: localhost
      port: 6379



file:
  COS:
    secretId: mock-access-key
    secretKey: mock-access-secret
    region: ap-nanjing
    bucketName: mock-bucket
    publicFolder:
    privateFolder:
    uploadExpiredTime: 60
    downloadExpiredTime: 1
    bucket-url-prefix: https://mock-bucket.cos.ap-nanjing.myqcloud.com/


server:
  port: 9090
